# ====================================
# IntellMeet Frontend — Environment Variables
# Copy this file to .env and fill in your values
# ====================================

# API base URL — when using Vite proxy (recommended for dev), leave as /api
# For production, set to your backend URL e.g. https://api.yourapp.com/api
VITE_API_URL=/api

# Socket.io server URL — leave empty to use the same origin (proxy handles it)
# For production: VITE_SOCKET_URL=https://api.yourapp.com
VITE_SOCKET_URL=
